name: Hello World get_item_images

on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Print Hello World
      run: echo "Hello World from Workflow get_item_images"
